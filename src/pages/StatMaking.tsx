import Head from "@/components/UI/Head";
import Layout from "@/components/UI/Layout";
import React from "react";

const StatMaking = () => {
  return (
    <Layout>
      <Head
        link="Stat Card Maker"
        desc="Please create your unique and personalized stat card in Makers, download it as a PNG file, and share it with others."
      />
      StatMaking
    </Layout>
  );
};

export default StatMaking;
